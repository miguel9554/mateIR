{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RTL Interface Domain Configuration",
  "description": "Describes clock domains, reset domains, and asynchronous interface inputs/outputs for an RTL module.",
  "type": "object",

  "required": ["module_name"],

  "$defs": {
    "signal_name": {
      "type": "string",
      "pattern": "^[a-zA-Z_][a-zA-Z0-9_$]*$",
      "description": "Exact interface port name."
    },

    "signal_wildcard": {
      "type": "string",
      "pattern": "^[a-zA-Z_][a-zA-Z0-9_$]*\\*$",
      "description": "Wildcard prefix representing a group of ports."
    },

    "signal_ref": {
      "anyOf": [
        { "$ref": "#/$defs/signal_name" },
        { "$ref": "#/$defs/signal_wildcard" }
      ]
    },

    "signal_with_attrs": {
      "description": "Signal reference with optional attributes.",
      "anyOf": [
        {
          "$ref": "#/$defs/signal_ref"
        },
        {
          "type": "object",
          "description": "Signal with additional attributes (e.g., synchronization target).",
          "minProperties": 1,
          "maxProperties": 1,
          "propertyNames": {
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_$]*$"
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "synchronized_into": {
                "type": "string",
                "description": "Clock domain into which this signal is synchronized."
              }
            },
            "additionalProperties": false
          }
        }
      ]
    }
  },

  "properties": {

    "module_name": {
      "type": "string",
      "description": "Name of the RTL module this configuration applies to.",
      "pattern": "^[a-zA-Z_][a-zA-Z0-9_$]*$"
    },

    "pure_combinational": {
      "type": "boolean",
      "description": "If true, this module is purely combinational: no flops, no clock domains. Port classification is skipped and cross-module sync_kind checks are suppressed for this module."
    },

    "clock_domains": {
      "type": "object",
      "description": "Mapping of clock domain names to their configuration.",

      "additionalProperties": {
        "type": "object",
        "title": "Clock Domain",
        "description": "Definition of a synchronous clock domain.",

        "required": ["polarity", "inputs_outputs"],

        "properties": {

          "input_name": {
            "$ref": "#/$defs/signal_name",
            "description": "Optional clock input port name in the RTL module interface. If omitted, the domain name is used."
          },

          "polarity": {
            "type": "string",
            "description": "Clock edge used by sequential logic in this domain.",
            "enum": ["posedge", "negedge"]
          },

          "inputs_outputs": {
            "type": "array",
            "description": "List of module interface inputs/outputs belonging to this clock domain.",
            "minItems": 1,
            "items": {
              "$ref": "#/$defs/signal_with_attrs"
            }
          }
        },

        "additionalProperties": false
      }
    },

    "resets": {
      "type": "object",
      "description": "Mapping of reset domain names to their configuration.",

      "additionalProperties": {
        "type": "object",
        "title": "Reset Domain",
        "description": "Definition of a reset interface signal.",

        "required": ["polarity"],

        "properties": {

          "signal_name": {
            "$ref": "#/$defs/signal_name",
            "description": "Optional reset input port name in the RTL module interface. If omitted, the reset domain name is used."
          },

          "polarity": {
            "type": "string",
            "description": "Reset polarity by level. 'positive' means active-high reset, 'negative' means active-low reset.",
            "enum": ["positive", "negative"]
          }
        },

        "additionalProperties": false
      }
    },

    "async_domain": {
      "type": "array",
      "description": "List of module interface inputs/outputs that are asynchronous to all defined clock domains.",
      "items": {
        "$ref": "#/$defs/signal_with_attrs"
      }
    }

  },

  "additionalProperties": false
}
