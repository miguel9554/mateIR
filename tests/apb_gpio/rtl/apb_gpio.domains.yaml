module_name: apb_gpio

resets:
  HRESETn:
    polarity: negative

clock_domains:
  HCLK:
    polarity: posedge
    inputs_outputs:
      - dft_cg_enable_i
      - P*
      - gpio_in_sync
      - gpio_out
      - gpio_dir
      - gpio_padcfg
      - interrupt

async_domain:
  - gpio_in
