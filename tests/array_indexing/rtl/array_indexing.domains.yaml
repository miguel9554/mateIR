module_name: array_indexing

resets:
  rst_n:
    polarity: negative

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - stim
      - packed_sel
      - unpacked_sel
      - slice_base
      - packed_word_q
      - packed_word_math
      - packed_elem_const
      - packed_slice_const
      - packed_slice_plus
      - packed_slice_minus
      - unpacked_flat_q
      - unpacked_elem_const
      - unpacked_elem_var
      - grid_elem_const
      - grid_elem_var
      - mixed_word_const
      - mixed_leaf_const
      - mixed_leaf_var
      - signed_sum
      - packed_equal
