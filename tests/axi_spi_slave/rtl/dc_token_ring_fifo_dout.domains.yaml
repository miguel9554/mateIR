module_name: dc_token_ring_fifo_dout

resets:
  rstn:
    polarity: negative

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - ready
      - data
      - valid
      - read_pointer

async_domain:
  - write_token
  - data_async
