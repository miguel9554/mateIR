module_name: counter

resets:
  a_rst:
    polarity: positive

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - reset
      - count
      - next_count
      - next_done
      - done
