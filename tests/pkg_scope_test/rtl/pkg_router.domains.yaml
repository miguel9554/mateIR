module_name: pkg_router

resets:
  rst_n:
    polarity: negative

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - dir_in
      - op_in
      - sched_in
      - weight_in
      - data_in
      - dir_out
      - op_out
      - routed_data
