module_name: pkg_scope_test

resets:
  rst_n:
    polarity: negative

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - op_in
      - src_in
      - dst_in
      - sched_in
      - dir_in
      - shape_in
      - weight_in
      - data_in
      - phase_out
      - result_out
      - dir_out
      - valid_out
