resets:
  rst:
    polarity: negative

clock_domains:
  clk:
    polarity: posedge
    input_name: clk_i
    inputs_outputs:
      - s_axis_*
      - m_axis_*
      - txd
      - tx_busy
      - rx_busy
      - rx_overrun_error
      - rx_frame_error
      - prescale

async_domain:
  - rxd
