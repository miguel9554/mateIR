module_name: uart

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - rst
      - s_axis_*
      - m_axis_*
      - txd
      - tx_busy
      - rx_busy
      - rx_overrun_error
      - rx_frame_error
      - prescale

async_domain:
  - rxd
