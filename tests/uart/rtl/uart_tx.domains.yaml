module_name: uart_tx

clock_domains:
  clk:
    polarity: posedge
    inputs_outputs:
      - rst
      - s_axis_tdata
      - s_axis_tvalid
      - s_axis_tready
      - txd
      - busy
      - prescale
